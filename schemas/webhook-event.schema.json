{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://osp.dev/schemas/webhook-event.schema.json",
  "title": "WebhookEvent",
  "description": "Webhook payload sent by providers for async provisioning updates and lifecycle events",
  "type": "object",
  "required": ["event_id", "event_type", "resource_id", "offering_id", "timestamp", "provider_signature"],
  "properties": {
    "event_id": {
      "type": "string",
      "description": "Unique identifier for this webhook event, used for idempotency"
    },
    "event_type": {
      "type": "string",
      "enum": [
        "provision.started",
        "provision.completed",
        "provision.failed",
        "deprovision.started",
        "deprovision.completed",
        "deprovision.failed",
        "credentials.rotated",
        "credentials.expiring",
        "resource.warning",
        "resource.suspended",
        "resource.resumed",
        "usage.threshold",
        "usage.report_ready",
        "payment.required",
        "payment.confirmed",
        "budget.alert",
        "budget.exceeded",
        "nhi.token_expiring",
        "nhi.token_rotated",
        "dependency.health_changed",
        "scorecard.updated",
        "environment.ttl_expiring",
        "environment.ttl_expired"
      ],
      "description": "Type of lifecycle event"
    },
    "resource_id": {
      "type": "string",
      "description": "The provisioned resource this event relates to"
    },
    "request_id": {
      "type": "string",
      "description": "The original provisioning request ID (for provision events)"
    },
    "offering_id": {
      "type": "string",
      "pattern": "^[a-z0-9-]+/[a-z0-9-]+$",
      "description": "The service offering this event relates to"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "When this event occurred (ISO 8601)"
    },
    "data": {
      "$ref": "#/$defs/EventData",
      "description": "Event-specific payload data"
    },
    "provider_signature": {
      "type": "string",
      "description": "Ed25519 signature by the provider over the canonical JSON of this event (excluding this field)"
    },
    "delivery_attempt": {
      "type": "integer",
      "minimum": 1,
      "default": 1,
      "description": "Delivery attempt number (increments on retries)"
    },
    "trace_id": {
      "type": "string",
      "description": "OpenTelemetry trace ID for correlating events with provisioning traces (v1.1)"
    }
  },
  "$defs": {
    "EventData": {
      "type": "object",
      "description": "Event-specific payload containing details about what happened",
      "properties": {
        "status": {
          "type": "string",
          "enum": ["provisioning", "active", "failed", "suspended", "deprovisioning", "deprovisioned"],
          "description": "Current status of the resource after this event"
        },
        "credentials": {
          "$ref": "#/$defs/CredentialBundleRef",
          "description": "New credential bundle (for provision.completed and credentials.rotated events)"
        },
        "fulfillment_proof": {
          "$ref": "#/$defs/FulfillmentProof",
          "description": "Proof of fulfillment (for provision.completed events)"
        },
        "error": {
          "$ref": "#/$defs/EventError",
          "description": "Error details (for *.failed events)"
        },
        "warning": {
          "$ref": "#/$defs/ResourceWarning",
          "description": "Warning details (for resource.warning events)"
        },
        "usage_threshold": {
          "$ref": "#/$defs/UsageThreshold",
          "description": "Usage threshold details (for usage.threshold events)"
        },
        "payment_details": {
          "$ref": "#/$defs/PaymentDetails",
          "description": "Payment-related details (for payment.* events)"
        },
        "budget_alert": {
          "$ref": "#/$defs/BudgetAlert",
          "description": "Budget alert details (for budget.* events) (v1.1)"
        },
        "nhi_event": {
          "$ref": "#/$defs/NHIEvent",
          "description": "NHI token lifecycle event details (for nhi.* events) (v1.1)"
        },
        "dependency_event": {
          "$ref": "#/$defs/DependencyEvent",
          "description": "Dependency health change details (for dependency.* events) (v1.1)"
        },
        "ttl_event": {
          "$ref": "#/$defs/TTLEvent",
          "description": "Environment TTL event details (for environment.* events) (v1.1)"
        },
        "dashboard_url": {
          "type": "string",
          "format": "uri",
          "description": "URL to the provider's dashboard for this resource"
        },
        "message": {
          "type": "string",
          "description": "Human-readable description of what happened"
        }
      }
    },
    "CredentialBundleRef": {
      "type": "object",
      "required": ["encrypted_payload", "encryption_method", "provider_signature"],
      "description": "Encrypted credential bundle delivered via webhook",
      "properties": {
        "encrypted_payload": {
          "type": "string",
          "description": "Base64url-encoded encrypted credential payload"
        },
        "encryption_method": {
          "type": "string",
          "enum": ["x25519-xsalsa20-poly1305", "aes-256-gcm"],
          "description": "Encryption algorithm used"
        },
        "ephemeral_public_key": {
          "type": "string",
          "description": "Ephemeral public key for key exchange (base64url)"
        },
        "nonce": {
          "type": "string",
          "description": "Encryption nonce (base64url)"
        },
        "provider_signature": {
          "type": "string",
          "description": "Ed25519 signature over the encrypted payload"
        }
      }
    },
    "FulfillmentProof": {
      "type": "object",
      "required": ["type", "timestamp"],
      "description": "Proof that provisioning was fulfilled",
      "properties": {
        "type": {
          "type": "string",
          "enum": ["api_key_delivery", "health_check", "signed_receipt"],
          "description": "Type of fulfillment proof"
        },
        "health_check_url": {
          "type": "string",
          "format": "uri",
          "description": "URL to verify the resource is alive"
        },
        "receipt_signature": {
          "type": "string",
          "description": "Provider signature over the receipt"
        },
        "receipt_payload": {
          "type": "string",
          "description": "Base64url-encoded canonical receipt data"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "When fulfillment was proven"
        }
      }
    },
    "EventError": {
      "type": "object",
      "required": ["code", "message"],
      "description": "Error details for failed events",
      "properties": {
        "code": {
          "type": "string",
          "description": "Machine-readable error code"
        },
        "message": {
          "type": "string",
          "description": "Human-readable error description"
        },
        "retryable": {
          "type": "boolean",
          "description": "Whether the failed operation can be retried"
        },
        "retry_after_seconds": {
          "type": "integer",
          "description": "Suggested number of seconds before retrying"
        }
      }
    },
    "ResourceWarning": {
      "type": "object",
      "required": ["warning_type", "message"],
      "description": "Warning about a resource condition",
      "properties": {
        "warning_type": {
          "type": "string",
          "enum": ["approaching_limit", "performance_degraded", "maintenance_scheduled", "credential_expiring", "payment_overdue", "budget_threshold", "ttl_expiring"],
          "description": "Category of warning"
        },
        "message": {
          "type": "string",
          "description": "Human-readable warning message"
        },
        "severity": {
          "type": "string",
          "enum": ["info", "warning", "critical"],
          "description": "Severity level of the warning"
        },
        "action_required_by": {
          "type": ["string", "null"],
          "format": "date-time",
          "description": "Deadline by which action should be taken, null if no deadline"
        }
      }
    },
    "UsageThreshold": {
      "type": "object",
      "required": ["dimension", "threshold_percent", "current_usage"],
      "description": "Details about a usage threshold being reached",
      "properties": {
        "dimension": {
          "type": "string",
          "description": "The usage dimension that triggered the threshold"
        },
        "threshold_percent": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "The threshold percentage that was reached"
        },
        "current_usage": {
          "type": "string",
          "description": "Current usage quantity as a string"
        },
        "limit": {
          "type": "string",
          "description": "The tier's limit for this dimension"
        },
        "unit": {
          "type": "string",
          "description": "Unit of measurement"
        }
      }
    },
    "PaymentDetails": {
      "type": "object",
      "required": ["amount", "currency"],
      "description": "Payment-related event details",
      "properties": {
        "amount": {
          "type": "string",
          "pattern": "^[0-9]+(\\.[0-9]{1,2})?$",
          "description": "Payment amount"
        },
        "currency": {
          "type": "string",
          "enum": ["USD", "EUR", "GBP", "USDC", "EURC"],
          "description": "Currency of the payment"
        },
        "payment_method": {
          "type": "string",
          "description": "Payment method used or required"
        },
        "due_by": {
          "type": ["string", "null"],
          "format": "date-time",
          "description": "Payment deadline (for payment.required events)"
        },
        "transaction_id": {
          "type": "string",
          "description": "Transaction identifier (for payment.confirmed events)"
        }
      }
    },
    "BudgetAlert": {
      "type": "object",
      "description": "Budget alert event details (v1.1)",
      "properties": {
        "budget_limit": {
          "type": "string",
          "pattern": "^[0-9]+(\\.[0-9]{1,2})?$",
          "description": "Budget limit amount"
        },
        "current_spend": {
          "type": "string",
          "pattern": "^[0-9]+(\\.[0-9]{1,2})?$",
          "description": "Current spend amount"
        },
        "percent_used": {
          "type": "integer",
          "description": "Budget utilization percentage"
        },
        "currency": {
          "type": "string",
          "enum": ["USD", "EUR", "GBP", "USDC", "EURC"],
          "description": "Budget currency"
        },
        "action": {
          "type": "string",
          "enum": ["alert", "throttle", "block"],
          "description": "Enforcement action taken"
        }
      }
    },
    "NHIEvent": {
      "type": "object",
      "description": "NHI token lifecycle event details (v1.1)",
      "properties": {
        "identity_id": {
          "type": "string",
          "description": "Non-human identity identifier"
        },
        "token_type": {
          "type": "string",
          "enum": ["bearer", "dpop", "mtls"],
          "description": "Token type"
        },
        "expires_at": {
          "type": "string",
          "format": "date-time",
          "description": "When the token expires"
        },
        "refresh_endpoint": {
          "type": "string",
          "description": "Endpoint to refresh the token"
        }
      }
    },
    "DependencyEvent": {
      "type": "object",
      "description": "Dependency health change event details (v1.1)",
      "properties": {
        "dependency_resource_id": {
          "type": "string",
          "description": "Resource ID of the dependency whose health changed"
        },
        "dependency_offering_id": {
          "type": "string",
          "description": "Offering ID of the dependency"
        },
        "health_status": {
          "type": "string",
          "enum": ["healthy", "degraded", "unhealthy", "unknown"],
          "description": "New health status of the dependency"
        },
        "affected_resources": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Resource IDs affected by this health change"
        }
      }
    },
    "TTLEvent": {
      "type": "object",
      "description": "Ephemeral environment TTL event details (v1.1)",
      "properties": {
        "ttl_remaining_seconds": {
          "type": "integer",
          "description": "Seconds remaining before TTL expiration"
        },
        "original_ttl_seconds": {
          "type": "integer",
          "description": "Original TTL duration in seconds"
        },
        "action": {
          "type": "string",
          "enum": ["warning", "expiring", "expired", "extended"],
          "description": "TTL lifecycle action"
        },
        "extension_available": {
          "type": "boolean",
          "description": "Whether the TTL can be extended"
        }
      }
    }
  }
}
